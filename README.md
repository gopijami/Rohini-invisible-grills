# Rohini Invisible Grills — Next.js + TypeScript

This is a mobile-first, SEO-friendly static Next.js website scaffolded with TypeScript and Tailwind CSS for Rohini Invisible Grills.

Quick start:

```bash
npm install
npm run dev
```

To build static export:

```bash
npm run export
```

Notes:
- Uses Tailwind CSS for styling.
- Configured for static export (`next export`).
